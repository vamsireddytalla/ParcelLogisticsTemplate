<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.talla.tsrtcparcelservice">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.OrderDeliverActivity"></activity>
        <activity android:name=".activities.AgentActivity" />
        <activity
            android:name=".activities.TrackOrderActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Utills.CaptureActivity"
            android:screenOrientation="portrait"
            android:stateNotNeeded="true"
            android:theme="@style/zxing_CaptureTheme" />
        <activity android:name=".activities.OrderStatusActivity" />
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.OrderDetailActivity" />
        <activity android:name=".activities.PlacedSucessfullyActivity" />
        <activity android:name=".activities.OrdersHistoryActivity" />
        <activity android:name=".activities.CreateShipmentActivity" />
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>